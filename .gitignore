# Local-only build state and credentials. Published .deb, .gpg and APT index
# files are intentionally tracked when GitHub Actions writes /debian to main.
/dist/
/upstream/
/.secrets/
*.asc
*.kbx
*.tmp
