#!/bin/sh
set -eu
if [ "$#" -eq 0 ]; then
    echo "Usage: stm32mp2-gpu-openvx-env COMMAND [ARGUMENT ...]" >&2
    exit 64
fi
export VIVANTE_SDK_DIR="@VIVANTE_SDK_DIR@"
export LD_LIBRARY_PATH="@LIBDIR@${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec "$@"
